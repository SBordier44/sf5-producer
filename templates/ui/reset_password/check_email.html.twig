{% extends 'base.html.twig' %}

{% block title %}Mot de passe perdu{{ parent() }}{% endblock %}

{% block body %}
    <section class="py-2 bg-light">
        <div class="container">
            <div class="row px-4 px-lg-5 py-lg-4 align-items-center">
                <div class="col-12">
                    <h3 class="h3 text-uppercase mb-0 text-center section-title">Mot de passe perdu</h3>
                </div>
            </div>
        </div>
    </section>
    <section>
        <div class="container">
            <div class="row mb-5">
                <div class="col-6 my-auto mx-auto">
                    <p>
                        Si un compte correspondant à votre e-mail existe, vous allez recevoir un email contenant un lien
                        qui vous permettra de réinitialiser votre mot de passe.<br>
                        Le lien expirera
                        dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
                        .
                    </p>
                    <p>Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <a
                                href="{{ path('security_forgot_password_request') }}">réessayer</a>.
                    </p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
